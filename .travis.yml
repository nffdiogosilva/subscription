language: python
python:
  - "3.7"
# command to install dependencies
install:
  - pip install pipenv
  - pipenv install --dev --verbose
# command to run tests
script:
  - pytest
after_success:
- coveralls